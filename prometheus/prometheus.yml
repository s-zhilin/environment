global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Пример цели в сети services — сервисы видны по имени контейнера.
  # Раскомментируйте и поправьте под свои экспортёры.
  # - job_name: node-exporter
  #   static_configs:
  #     - targets: ["node-exporter:9100"]
